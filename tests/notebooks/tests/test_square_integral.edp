
int n = 100;


mesh Th = square(2*n,n,[2*(x-0.5),1*(y-0.5)]);

fespace Vh(Th,P2);

Vh f = sin(x*y)^2;

real res = int2d(Th)( f );


cout << "Hello" << endl;
cout << "Function maximum: " << f[].max << endl;
cout << "integral result: "  << res << endl;

// plot([ux,uy],wait=1,ps="lamevect.eps",coef=coef);

// plot(Th,f,wait=1);


