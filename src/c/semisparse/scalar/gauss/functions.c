


// ****************************************************************************************************
fessoti_t fessoti_atanh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_atanh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_asinh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_asinh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_acosh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_acosh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_tanh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_tanh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_cosh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_cosh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_sinh(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_sinh_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------








// ****************************************************************************************************
fessoti_t fessoti_asin(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_asin_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_acos(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_acos_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_atan(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_atan_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------





// ****************************************************************************************************
fessoti_t fessoti_tan(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_tan_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_cos(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_cos_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_sin(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_sin_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------












// ****************************************************************************************************
fessoti_t fessoti_logb(fessoti_t* num, double base, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_logb_to(num, base, &res, dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_log10(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_log10_to(num, &res, dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_log(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_log_to(num, &res, dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_exp(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_exp_to(num, &res, dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------









// ****************************************************************************************************
fessoti_t fessoti_cbrt(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_cbrt_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_sqrt(fessoti_t* num, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);
    
    fessoti_sqrt_to(num, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
fessoti_t fessoti_pow(fessoti_t* num, double e, dhelpl_t dhl){

    fessoti_t res = fessoti_init();

    res = fessoti_createEmpty_bases(num->nip, 0, 0, dhl);

    fessoti_pow_to(num, e, &res, dhl);
    
    return res;

}
// ----------------------------------------------------------------------------------------------------



























// ****************************************************************************************************
void fessoti_atanh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_atanh_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_asinh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_asinh_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_acosh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_acosh_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------






// ****************************************************************************************************
void fessoti_tanh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_tanh_to(&num->p_data[i], &res->p_data[i], dhl);

    }



}
// ----------------------------------------------------------------------------------------------------


// ****************************************************************************************************
void fessoti_cosh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_cosh_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_sinh_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_sinh_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------








// ****************************************************************************************************
void fessoti_asin_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_asin_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_acos_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_acos_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_atan_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_atan_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_tan_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_tan_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_cos_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_cos_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_sin_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_sin_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------









// ****************************************************************************************************
void fessoti_logb_to(fessoti_t* num, double base, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_logb_to(&num->p_data[i], base, &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_log10_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_log10_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_log_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_log_to(&num->p_data[i], &res->p_data[i], dhl);

    } 

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_exp_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_exp_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------






// ****************************************************************************************************
void fessoti_cbrt_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_cbrt_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_sqrt_to(fessoti_t* num, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_sqrt_to(&num->p_data[i], &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_pow_to(fessoti_t* num, double e, fessoti_t* res, dhelpl_t dhl){

    uint64_t i;

    fessoti_dimCheck(num,res);

    for( i = 0; i<num->nip; i++){
        
        ssoti_pow_to(&num->p_data[i], e, &res->p_data[i], dhl);

    }

}
// ----------------------------------------------------------------------------------------------------


























// ****************************************************************************************************
semiotin_t fessoti_integrate_r(coeff_t num, fessoti_t* w, dhelpl_t dhl){

    semiotin_t  res = ssoti_init(); // res = 0.0

    fessoti_integrate_r_to(num, w, &res,  dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_integrate_r_to(coeff_t num, fessoti_t* w, semiotin_t* res, dhelpl_t dhl){

    uint64_t i = 0;

    semiotin_t tmp = ssoti_get_tmp( 1, fessoti_get_order( w ), dhl);

    // res may not come as zero, Modify this?
    ssoti_set_r(0,&tmp,dhl);

    // The loop for every element in num.
    for (i = 0; i < w->nip; i++){
        
        ssoti_sum_oo_to(  &tmp, &w->p_data[i], &tmp, dhl);

    }

    ssoti_mul_ro_to( num, &tmp, res, dhl);


}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
semiotin_t fessoti_integrate_o(semiotin_t* num, fessoti_t* w, dhelpl_t dhl){

    semiotin_t  res = ssoti_init(); // res = 0.0

    fessoti_integrate_o_to(num, w, &res,  dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_integrate_o_to(semiotin_t* num, fessoti_t* w, semiotin_t* res, dhelpl_t dhl){

    uint64_t i = 0;

    semiotin_t tmp = ssoti_get_tmp( 1, fessoti_get_order( w ), dhl);

    // res may not come as zero, Modify this?
    ssoti_set_r(0, &tmp, dhl);

    // The loop for every element in num.
    for (i = 0; i < w->nip; i++){
        
        ssoti_sum_oo_to(  &tmp, &w->p_data[i], &tmp, dhl);

    }

    ssoti_mul_oo_to( num, &tmp, res, dhl);


}
// ----------------------------------------------------------------------------------------------------


// ****************************************************************************************************
semiotin_t fessoti_integrate_f(fessoti_t* num, fessoti_t* w, dhelpl_t dhl){

    semiotin_t  res = ssoti_init(); // res = 0.0

    fessoti_integrate_to(num, w, &res,  dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_integrate_f_to(fessoti_t* num, fessoti_t* w, semiotin_t* res, dhelpl_t dhl){

    uint64_t i = 0;
    semiotin_t tmp = ssoti_get_tmp( 1, fessoti_get_order( w ), dhl);

    // Check dimensions.
    fessoti_dimCheck( num, w);


    // res may not come as zero, Modify this?
    ssoti_set_r(0,&tmp,dhl);

    // The loop for every element in num.
    for (i = 0; i < num->nip; i++){
        
        ssoti_gem_oo_to( &w->p_data[i], &num->p_data[i], &tmp, &tmp, dhl);

    }

    ssoti_set_o(&tmp, res, dhl);

}
// ----------------------------------------------------------------------------------------------------


// ****************************************************************************************************
semiotin_t fessoti_integrate(fessoti_t* num, fessoti_t* w, dhelpl_t dhl){

    semiotin_t  res = ssoti_init(); // res = 0.0

    fessoti_integrate_to(num, w, &res,  dhl);

    return res;

}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void fessoti_integrate_to(fessoti_t* num, fessoti_t* w, semiotin_t* res, dhelpl_t dhl){

    uint64_t i = 0;
    semiotin_t tmp = ssoti_get_tmp( 1, fessoti_get_order( w ), dhl);

    // Check dimensions.
    fessoti_dimCheck( num, w);


    // res may not come as zero, Modify this?
    ssoti_set_r(0,&tmp,dhl);

    // res may not come as zero, Modify this?

    // The loop for every element in num.
    for (i = 0; i < num->nip; i++){
        
        ssoti_gem_oo_to( &w->p_data[i], &num->p_data[i], &tmp, &tmp, dhl);

    }

    ssoti_set_o(&tmp, res, dhl);
}
// ----------------------------------------------------------------------------------------------------