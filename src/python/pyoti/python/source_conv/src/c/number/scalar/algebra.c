
// Division.

// ****************************************************************************************************
{num_type} {num_func}_div_ro(coeff_t num, {num_type}* den){{

    
    {num_type} inv = {num_func}_pow(den,-1);
    {num_type} res = {num_func}_mul_ro(num,&inv);

    return res;

}}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
{num_type} {num_func}_div_oo({num_type}* num, {num_type}* den){{

    
    {num_type} inv = {num_func}_init();
    {num_func}_pow_to(den,-1,&inv);
    {num_type} res = {num_func}_mul_oo(num,&inv);
    
    return res;

}}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
{num_type} {num_func}_div_or({num_type}* num, coeff_t val){{

    return {num_func}_mul_ro(1.0/val,num);

}}
// ----------------------------------------------------------------------------------------------------

{algebra_src}