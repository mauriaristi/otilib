
// Division.
// ****************************************************************************************************
void {num_func}_div_ro_to(coeff_t num, {num_type}* den, {num_type}* res){{

    {num_type} inv = {num_func}_get_rtmp(6, den->order);
    {num_func}_pow_to( den, -1, &inv);
    {num_func}_mul_ro_to(num,&inv,res);

}}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void {num_func}_div_oo_to({num_type}* num, {num_type}* den, {num_type}* res){{

    {num_type} inv = {num_func}_init();
    {num_func}_pow_to( den, -1, &inv);
    {num_func}_mul_oo_to( num, &inv, res);

}}
// ----------------------------------------------------------------------------------------------------

// ****************************************************************************************************
void {num_func}_div_or_to({num_type}* num, coeff_t val, {num_type}* res){{

    {num_func}_mul_ro_to(1.0/val, num, res);

}}
// ----------------------------------------------------------------------------------------------------
